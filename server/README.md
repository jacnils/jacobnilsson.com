# Note

This directory contains a prebuilt test server which you can use on Windows, since Windows sucks to compile... well, anything really, on.

If you use a proper Unix system, compiling stuff is not a pain in the ass and thus, you can compile/install one yourself.